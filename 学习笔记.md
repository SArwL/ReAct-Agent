# Agent 开发学习笔记

> 记录 2026-08-18 与 Claude Code 一起从零搭 ReAct Agent 的过程中的核心认知。
> 配套代码：`ReActAgent.py`（纯文本 ReAct）、`tool/`（工具包）、`prompt_template.py`、`setting.json`。

---

## 一、什么是 Agent

**一句话定义**：Agent 是一个**自己决定"下一步做什么"的程序**——它不断地"想、做、看"，循环往复，直到它判断任务完成。

**核心骨架**（你 `run()` 里的 `while True`）：

```
思考（LLM 决策）→ 行动（工具）→ 观察（结果喂回）→ 再思考 → ... → 结束
```

**三个核心组件 + 一个隐形组件**：

| 组件 | 角色 | 对应代码 |
|---|---|---|
| 决策 | 大脑：决定下一步 | `client.messages.create` |
| 行动 | 手：真的干活 | `tool/` 包 + `execute_tool` |
| 观察 | 眼睛：看行动结果 | observation 喂回循环 |
| 记忆（隐形） | 延续上下文 | `messages` 消息历史 |

**Agent vs 普通程序**：普通程序是确定性的（`if 下雨: 打伞`）；Agent 是非确定性的循环，每一步根据上一步的结果由模型**当场判断**。

**核心认知**：**Agent 的本质不是模型，是那个循环。** 模型只是循环里的"决策部件"，换 GPT/Claude/DeepSeek，循环照样成立。

**Agent 工程 = 在"聪明的但会犯错的决策者"外面修好护栏**（容忍不确定性、设边界、加校验）。

---

## 二、提示词的作用

**一句话定位**：提示词是**代码和模型之间唯一的"契约文件"**——你的代码怎么解析，提示词就得怎么教。

**五大职责**：

| 职责 | 说明 |
|---|---|
| ① 身份 | 模型知道自己是谁、干什么 |
| ② 流程 | 教它按 ReAct 循环工作 |
| ③ 协议 | **Agent 特有、最关键**：规定输出格式（`<thought>`/`<action>`/`<final_answer>`），代码才能解析 |
| ④ 纪律 | 不编数据、查不到明说、格式严格 |
| ⑤ 上下文 | 运行时动态填充：工具列表 / 系统环境 / 文件列表 |

**静态 + 动态**：静态部分（规则）写死；动态部分（`${tool_list}` 等占位符）每次运行时由 `Template(...).substitute(...)` 填充。

**核心认知**：提示词不是写给模型看的散文，是写给"模型 + 你的代码"整个系统的**接口文档**。两边格式一错位，Agent 就断。

---

## 三、你的文本协议 vs 主流原生 tool_use

**共同点**：提示词三职责（身份/纪律/动态上下文）主流 Agent 全都有。

**差异点**：主流不用文本标签，用 API 原生 tool_use。

| | 你的手写文本协议 | 主流原生 tool_use |
|---|---|---|
| 模型怎么表达"要调工具" | 输出 `<action>` 标签文本 | 返回结构化 `tool_use` 块 |
| 代码怎么接 | 正则抠标签 + 解析 JSON | SDK 直接给 dict |
| 格式出错率 | 高（漏标签/写错格式） | 低（API 强制结构） |
| 要多写代码 | 解析器、自纠错逻辑 | 几乎不用 |
| 可控性/可见性 | 每个字符都看得见 | 黑盒 |

**结论**：你手写的是"穷人版"，但它是绝佳的教学版本——能看见循环的每一环。学完手写版再看原生的，一通百通。

---

## 四、原生 tool_use 原理

**一句话**：tool_use = **在"生成时"就受约束的结构化输出**——服务端在解码时用 JSON Schema 约束 + 校验，格式不会坏。

**约束发生在"生成时" vs 你的"生成后解析"**：格式不合法的 token 序列在服务端就被拦下/重写，根本不会到你手里。

**协议的四条规则**：

| 规则 | 说明 | 对应你文本协议的写法 |
|---|---|---|
| ① 能力契约 | `TOOLS` 的 JSON Schema = 菜单 | `${tool_list}` 占位符 |
| ② 握手信号 | `stop_reason == "tool_use"` | `extract_tag(text, "action")` 判断 |
| ③ 回执单号 | `tool_use_id` 把调用和结果绑定 | `observation:` 前缀（但无防串台能力） |
| ④ 轮替规则 | assistant(tool_use) → user(tool_result) → ... | `messages.append` 两行 |

**三方分工**：模型负责"决定调不调 + 构造参数"（智能），API 负责"保证格式合法"（结构），你负责"执行 + 喂回结果"（行动）。

**重要认知**：最初被清空的 `agent.py`（get_weather 版）用的就是原生 tool_use——`block.type == "tool_use"` 那句。你当时写的就是主流做法，只是当时不知道。

---

## 五、微调能强制 LLM 按协议输出吗

**能提高概率，不能"强制"**。没有任何训练方法能保证 100% 遵守格式。

**四层"让模型听话"的手段**：

| 层级 | 手段 | 可靠度 | 成本 |
|---|---|---|---|
| L0 | 只靠提示词 | 低 | 0 |
| L1 | 提示词 + 自纠错循环 | 中 | 低 |
| L2 | **原生结构化输出 / tool_use** | 高 | 低 |
| L3 | 微调 | 高但**不绝对** | 高 |
| L4 | 约束解码（grammar-guided） | 接近硬保证 | 高，需自托管 |

**关键**：想"强制格式"用 L2（免费）；想"硬保证"用 L4；想"塑造行为倾向"才轮到 L3 微调。

**微调真正值得做的场景**：特定窄任务的行为塑造、把大模型能力蒸馏到小模型降本、提示词到达天花板时。

**信号**：改了几十版提示词、加了几层自纠错，模型还在同一个点稳定犯错——那时才是微调入场时机。需要可微调的开源模型 + 成百上千条高质量样本 + GPU。

---

## 六、学习路线（7 阶段）

1. **健壮性**：max_steps 上限、API 异常重试、轨迹日志
2. **多轮对话**：messages 提升为成员变量、token 计数、历史裁剪
3. **安全收口**：命令黑名单、workspace 路径沙箱、评估 run_python
4. **原生 tool_use**：重写一版，对比手写协议的优缺点
5. **流式输出**：client.messages.stream，思考/正文分流显示
6. **长期记忆 / RAG**：历史摘要存储 + 检索注入
7. **评估与测试**：设计测试用例集、轨迹打分、防退化回归

> 前 4 个阶段都是在现有代码上加几十行就能见效的，性价比最高。学完阶段 4 才算真正理解 Agent 技术全貌，之后看任何框架（LangChain 等）都是一眼通透。

> **进度（2026-08-19）**：阶段 1 的 max_steps 循环保护已落地（上限从 setting.json 读取，默认 30，到点兜底返回）；阶段 3 的「命令黑白名单」部分已落地（三层安全判定，见第九节）；workspace 路径沙箱、多轮对话、原生 tool_use 待做。

---

## 七、当前项目状态速览（2026-08-19）

- `MyAgent.py` — 入口，交互式提问
- `ReActAgent.py` — ReActAgent 类：`for` 循环（max_steps 上限）+ 三层安全判定 + 自纠错 + 宽容解析（未闭合标签）+ 流式接口 + 截断自纠错
- `tool/` — file_tools / shell_tools / code_tools / security.py（三层判定）；weather_tools 已移除
- `security.json` — 命令/工具黑白名单（配置与代码分离，fail-closed）
- `prompt_template.py` — 提示词模板（含动态占位符 + 安全策略说明）
- `setting.json` — LLM 配置（deepseek-v4-flash，明文 Key）+ `max_steps`（循环上限，默认 30）
- 待办：workspace 路径沙箱、多轮对话

---

## 八、MCP 和 Agent 的关系（概念课 2026-08-19）

**一句话**：Agent 是"怎么自主决策干活"的**程序范式**，MCP 是"怎么把工具接通"的**通信协议**。二者在不同维度，**正交，不互相包含**。

| 维度 | Agent | MCP |
|---|---|---|
| 本质 | 程序/架构范式（决策循环） | 通信协议/标准（连接） |
| 回答的问题 | 怎么一步步自主完成任务？ | 模型应用怎么发现并调用外部工具/数据？ |
| 有智能吗 | **有**——核心是 LLM + 决策循环 | **没有**——纯管道和契约 |
| 例子 | ReAct 循环、你的 ReActAgent | MCP SDK、MCP Server |

**三个独立存在的证明**：
- 无 MCP 的 Agent = 你现在这个项目（手写协议 + tool 包照样是 agent）；
- 无 Agent 的 MCP = 一段脚本直调 MCP Server 的工具，无决策循环，不是 agent；
- 二者组合 = Agent 当 MCP Client，连多个 MCP Server。

**你的手搓实现 ↔ MCP 对照**：

| 你的实现 | MCP |
|---|---|
| `tool/` 包（name/description/func） | MCP Server 工具定义（+ inputSchema） |
| `get_tools()` 拼进 prompt | `tools/list` |
| `execute_tool(name, args)` | `tools/call` |
| XML `<action>工具({...})</action>` + 正则 | JSON-RPC 结构化消息 |
| `ReActAgent` | MCP Client |
| `file_list` / workspace | resources（资源） |

**记忆点**：Agent 是"大脑 + 决策"，MCP 是"神经 + 接口"。大脑决定要不要用神经，神经本身不思考。MCP 只是换工具的"接线方式"，agent 的循环和决策一行都不用改。

---

## 九、安全收口实战：三层安全判定（对应路线图阶段 3）

**为什么**：`run_command` 是 `shell=True` 任意命令执行，之前只有"每次人工确认"一层闸门，不区分命令风险。

**三层判定**（`execute_tool` 之前，ReActAgent.run 里）：
- **allow（白名单）**：只读/查询类命令 + read_file → 打印概要**直接执行**，不弹确认
- **deny（黑名单）**：format/shutdown/taskkill 等破坏类 → **拒绝**，原因喂回 LLM 重新规划（循环继续）
- **ask（其余）**：write_file / run_python / del 等 → 打印概要 + y/n 人工确认（拒绝则 return -1）

**设计要点**：
- `classify_call()` 返回 (verdict, reason)；`summarize_call()` 生成概要——**显示命令/路径，隐藏长内容**（文件内容、代码），避免终端刷屏；
- **fail-closed**：security.json 缺失 → 白名单空集合，全落 ask/deny，绝不静默放行；
- **命令链/重定向操作符（& | > ; 等）判 ask 而非 allow**——防止白名单名掩护危险命令（`dir & del x` 不能被自动放行）；
- 配置与代码分离：改名单只改 `security.json`，不动代码；
- del/copy/ren/move 从黑名单移出 → ask（开发常用命令交人工判断，2026-08-19 用户决定）。

**核心认知**：黑白名单是"工具执行的策略层"，与模型、协议无关。它把"能做什么"从模型自由发挥里收回来，变成**可配置、可审计的策略**。

---

## 十、DeepSeek 实战踩坑（2026-08-19）

1. **非流式 10 分钟硬上限**：anthropic SDK 按 `max_tokens` 估算时长，超过 10 分钟直接报错（`Streaming is required...`）。`max_tokens=100000` 必触发 → 改用 `messages.stream()`。`get_final_message()` 返回结构与非流式一致，解析代码不用改。
2. **原生 thinking 块"抢走"思考**：DeepSeek 的推理进 thinking 块，模型是否再在 text 里写 `<thought>` 标签是**随机的**（简单问题基本不写）。修复：thought 显示做回退——先取 `<thought>` 标签，取不到就读 thinking 块。action/final_answer 仍只解析 text 块。
3. **输出截断**：`stop_reason in ("max_tokens", "length")`（DeepSeek 端点可能返回原生 "length"）→ 残缺内容喂回重输出。
4. **提示词角色声明**：开头明确"你是智能体而不只是对话模型，工具调用会在真实机器上执行"，能显著降低模型"光说不做"的倾向。
