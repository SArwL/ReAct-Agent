# 项目架构 ↔ 前沿 AI 概念对照

> 把 `D:\PycharmProjects\Agent` 这套手搓 ReAct Agent 的每一块，对应到当前行业（Anthropic、OpenAI、LangChain、MCP 生态）的标准概念。
> 目标：当你看到任何一篇 agent 技术文章、任何框架文档时，能立刻认出"这个我在我的项目里写过"。
> 日期：2026-08-19

---

## 一、全景：一条数据流对应整个行业栈

```
用户提问
  │
  ▼
MyAgent.py（入口/CLI）
  │
  ▼
ReActAgent.run()  ←←←← 这就是 Harness + Agent Executor
  │  ① 流式调用 LLM（messages.stream）
  │  ② 解析文本协议（extract_tag / parse_action）← 手写版 tool_use
  │  ③ 打印思考（reasoning 可见性）
  │  ④ 有 <final_answer>？→ 返回答案
  │  ⑤ 有 <action>？→ 三层安全判定（护栏）→ 人工确认（审批）
  │  ⑥ tool.execute_tool → 真实工具 → 操作系统
  │     观察结果 observation 喂回（环境反馈）
  │  循环直到 max_steps（终止保证）
  ▼
最终答案 / 兜底消息
```

## 二、核心对照表

| 你的组件 | 对应前沿概念 | 行业里的叫法 |
|---|---|---|
| `run()` 的 `for` 循环 + 调度判断 | **Agent 执行器** | AgentExecutor（LangChain）、agent loop |
| 整个 `ReActAgent` 类 + 所有脚手架 | **Harness**（运行框架） | Claude Code、各 agent runtime |
| `<action>工具名({"参数": 值})</action>` + 正则 | **tool_use / function calling**（文本版） | Anthropic tool_use、OpenAI function calling |
| `tool/` 的 name/description/func 契约 | **Tool Schema** | JSON Schema 工具定义 |
| `get_tools()` 拼进 prompt | **工具发现 / 工具注册表** | MCP `tools/list` |
| `execute_tool(name, args)` | **工具调度 / 执行器** | MCP `tools/call`、tool runner |
| `tool/` 包 + XML 协议 整体 | **MCP（手搓迷你版）** | MCP Server + Client |
| `prompt_template.py` | **系统提示词 / 提示词工程** | system message、few-shot |
| `${tool_list}` `${operating_system}` 等占位符 | **动态上下文注入** | 上下文装配、tool discovery→prompt |
| `messages` 消息列表 | **短期记忆 / 上下文管理** | conversation buffer、context window |
| `observation:` 喂回 | **环境反馈 / ReAct 观察** | ReAct observation |
| thinking 块回退显示 | **思维链可见性 / 推理提取** | CoT、reasoning blocks |
| `security.py` + `security.json` 三层 | **安全护栏 / 权限策略** | Claude Code 的 allow/ask/deny 权限模型 |
| `workspace/` 目录 | **沙箱 / 工作区** | sandbox、workspace、cwd |
| `max_steps` + 兜底返回 | **终止保证 / 健壮性** | max_iterations、timeout |
| 错误喂回自纠错 | **自纠错 / Reflexion** | self-correction、reflexion |
| 截断自纠错（stop_reason） | **流式截断处理** | max_tokens 截断恢复 |
| `MyAgent.py` | **应用入口 / 前端** | controller、CLI frontend |
| `setting.json` | **运行时配置** | LLM provider config |
| DeepSeek 原生 thinking 块 | **推理模型 / 思维链（模型侧）** | reasoning model、CoT |

---

## 三、你点名要问的四个概念，单独展开

### 1️⃣ 哪一部分是「Agent 执行器」（Agent Executor）？

**`ReActAgent.run()` 里那段 `for step in range(max_steps)` 循环本身。**

它是"决策调度器"：每轮决定——
- 模型这轮输出是答案（`<final_answer>`）→ 结束；
- 还是要调工具（`<action>`）→ 调度给工具执行 → 拿结果喂回 → 继续。

在 LangChain 里这个角色叫 `AgentExecutor`；在官方 API 的新版里叫 agent loop。**执行器只管"循环和调度"，它自己不聪明**——聪明的部分是模型，它是骨架。

### 2️⃣ 哪一部分是「Harness」？

**整个 `ReActAgent.py`——比执行器大一圈的壳。**

Harness = 让模型"在一个环境里能干活的全部支撑"：

| 你写的 | 支撑什么 |
|---|---|
| messages 管理 + 流式调用 | 模型怎么被调用 |
| prompt 构建（模板 + 动态填充） | 模型拿到什么上下文 |
| tool/ 接入 + execute_tool | 模型有什么"手" |
| 三层安全判定 | 什么能碰、什么不能碰 |
| max_steps / 自纠错 / 截断处理 | 出错怎么办、何时终止 |
| 打印思考/执行过程 | 用户怎么看懂 |

Claude Code 的官方定位就是"harness + 模型 + 工具"——**你的 ReActAgent.py 就是这个 harness**，只不过它是纯手写的、跑在终端里。这也是第一课"Agent 的本质是循环而非模型"的延续：**换掉 harness 里的模型（DeepSeek→GPT→Claude），循环照样转；harness 才是你写的"工程"。**

**执行器 vs Harness**：执行器是 harness 里驱动循环的那一小块调度代码；harness 是包含它的整个运行框架。类比：执行器是活塞，harness 是发动机+车身+仪表盘。

### 3️⃣ 哪一部分是「tool_use」？

**`<action>工具名({"参数": 值})</action>` 的整个"模型→代码"契约 + `parse_action` 解析。**

tool_use 的本质是：**模型输出的工具调用是结构化、可解析的**，而不是一句自然语言"帮我查一下文件"。你用手写 XML 模拟了这一点：

| tool_use 原生机制 | 你的文本版 |
|---|---|
| 模型返回结构化 `tool_use` block | 模型输出 `<action>工具(...)</action>` |
| SDK 解析成 dict | `parse_action` 正则解析成 dict |
| 服务端校验格式（不会坏） | 你靠自纠错兜格式（会坏，你踩过坑） |
| `stop_reason == "tool_use"` 握手 | `extract_tag(text, "action")` 判断 |

**结论**：你写的是 tool_use 的"穷人版"——同一概念，只是把格式约束从服务端搬到了你自己的解析+自纠错代码里。这是你路线图**阶段 4** 要升级成原生版的部分。

### 4️⃣ 哪一部分相当于「MCP」？

**`tool/` 包 + XML 协议合起来 = 一个手搓的迷你 MCP。**

| MCP 概念 | 你的实现 |
|---|---|
| MCP Server（暴露工具的一方） | `tool/` 包（每个工具注册 name/description/func） |
| MCP Client（消费工具的一方） | `ReActAgent` |
| `tools/list`（发现工具） | `get_tools()` |
| `tools/call`（调用工具） | `execute_tool(name, args)` |
| 工具 inputSchema（参数约束） | "参数用 JSON dict"这条铁律（但没形式化成 schema） |
| 传输协议（JSON-RPC） | 你的 XML 文本协议 |
| resources（可读数据） | `file_list` / `workspace` |
| 远程传输（HTTP/SSE） | ❌ 你没有——只能同进程函数调用 |

**一句话**：MCP = 把你的"tool 包 + XML 协议"**标准化、结构化、可远程化**。你的 ReActAgent 已经是"MCP 架构"的思想，只差把协议换成标准实现。

---

## 四、几个你可能没想到的对照

- **三层安全判定（allow/deny/ask）正好等于 Claude Code 的权限模型**——Claude Code 内置了三种 permission：allow（直接执行）、ask（问用户）、deny（拒绝）。你手写的 security.py 和它是同一个设计，这是很好的印证。
- **错误喂回自纠错 = 学术界的 Reflexion 范式**——让模型看到自己的错误再改，是当前前沿 agent 的核心机制之一，你已经实现了。
- **thinking 块显示 = 思维链（CoT）可见性**——DeepSeek 原生给了你 reasoning，你只是把它展示出来。前沿 agent 把这叫 "reasoning transparency"。
- **workspace/ = 沙箱**——Claude Code 有独立 sandbox；你的 workspace 是雏形（还没限制死，是下一步要做的）。
- **max_steps + 兜底 = 终止保证（termination guarantee）**——任何生产级 agent 都必须有，你刚补上。

---

## 五、你现在 vs 生产级前沿：差距在哪

| 维度 | 你的实现 | 前沿/生产 |
|---|---|---|
| 工具调用格式 | 文本 XML + 正则（会格式错） | 原生 tool_use（结构化，服务端保证） |
| 工具接入方式 | 同进程函数 | MCP（跨进程、跨机器、标准化） |
| 记忆 | messages 一次性（用完即弃） | 持久化 + 历史裁剪 + 摘要 + RAG |
| 安全 | 黑白名单 + 人工确认 | 沙箱隔离 + 权限系统 + 完整审计日志 |
| 评估 | 无 | 测试用例集 + 轨迹打分 + 回归防退化 |
| 部署形态 | 单进程 CLI | 异步服务 + 多用户并发会话 + 长任务队列 |
| 上下文 | 全量塞进 messages | 动态裁剪/检索注入（RAG） |

**结论**：你已拥有一个**真实、完整、可运行的 Harness + 手写 tool_use + 迷你 MCP + 护栏**的最小实现。剩下的差距，恰好就是你 7 阶段路线图的后半段（原生 tool_use → RAG → 评估 → 部署）。

---

## 六、一句话记忆口诀

> **你的项目 = 一个手写的 Harness，里面跑着一个 Agent Executor，通过手写 tool_use 协议驱动你的迷你 MCP（tool/ 包），外面套着 allow/ask/deny 护栏和 max_steps 刹车。**
> 把这句话里的"手写"换成"标准实现"，就是今天工业界的 agent 架构。
